{
  "mcpServers": {
    "plane": {
      "type": "stdio",
      "command": "uvx",
      "args": ["plane-mcp-server@0.3.1", "stdio"],
      "env": {
        "PLANE_BASE_URL": "https://plane.xgrunt.com",
        "PLANE_API_KEY": "${PLANE_API_KEY}",
        "PLANE_WORKSPACE_SLUG": "cfoperator"
      }
    }
  }
}
