"""Optional integrations for the event runtime.

Nothing in the core imports this package. A plugin here runs only when it is
named in ``CFOP_EVENT_RUNTIME_PLUGINS`` (see ``event_runtime/external_plugins.py``).
"""
