# Core web framework
flask>=3.0.0
flask-sock>=0.7.0
websockets>=12.0
waitress>=3.0.0

# LLM providers
anthropic>=0.18.0
google-generativeai>=0.3.0
openai>=1.12.0

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
pgvector>=0.2.0

# Monitoring tools
docker>=7.0.0
prometheus-client>=0.19.0
requests>=2.31.0

# Utilities
pyyaml>=6.0
python-dotenv>=1.0.0
slack-sdk>=3.26.0

# For embeddings (Ollama via OpenAI-compat API)
# numpy for vector operations (used by embedding_service)
numpy>=1.26.0

# MCP (Model Context Protocol) server
mcp>=0.9.0
