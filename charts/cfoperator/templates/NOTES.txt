CFOperator is installing ({{ .Values.profile }} profile).

1. Console — port-forward and open http://localhost:{{ .Values.console.service.port }}

     kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "cfoperator.fullname" . }}-agent {{ .Values.console.service.port }}:{{ .Values.console.service.port }}

2. Admin credentials — username {{ .Values.admin.username | quote }}.
{{- if .Values.admin.password }}
   Password: the one you set in admin.password.
{{- else }}
   The password was GENERATED and printed once by the bootstrap Job:

     kubectl -n {{ .Release.Namespace }} logs job/{{ include "cfoperator.fullname" . }}-bootstrap

   Save it now — it is not stored in plaintext anywhere. (The log survives
   until the next `helm upgrade` re-creates the Job.)
{{- end }}

3. Watch the first triage/investigation:

     kubectl -n {{ .Release.Namespace }} logs deploy/{{ include "cfoperator.fullname" . }}-agent -f

{{- if not .Values.alertmanager.url }}

Note: alertmanager.url is unset, so no alerts are polled — the agent still
answers chat and runs sweeps. Point it at your Alertmanager to complete the
alert -> triage -> investigate loop.
{{- end }}
{{- if eq .Values.profile "remediate" }}

Remediate profile: auto-eligible queue rows spawn executor Jobs in
{{ .Release.Namespace }} which open PRs against {{ .Values.remediate.gitRepo }}.
The human gate is the PR merge button — nothing mutates your cluster.
{{- end }}
